# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Build info
*.tsbuildinfo

# Local env
.env
.env*.local

# macOS
.DS_Store

# Scaffold demo posts — kept locally as a formatting reference, never deployed.
# Drafts now render in production builds, so these must stay out of git.
posts/writing-reference.mdx
posts/unfinished-draft.mdx
